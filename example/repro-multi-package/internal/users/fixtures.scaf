// Fixture functions for users package
// 
// NOTE: After merge, GetUser from queries.scaf is in the same file,
// so we can reference it directly without the import prefix.
// The same-package import warning will be emitted during merge.

import queries "./queries"

fn CreateTestUsers() `
CREATE (:User {id: 1, name: "Alice", email: "alice@test.com"})
CREATE (:User {id: 2, name: "Bob", email: "bob@test.com"})
`

fn CleanupUsers() `
MATCH (u:User) DETACH DELETE u
`
